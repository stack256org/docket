# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
__MACOSX

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env files (ignore all real env files; the *.example templates ARE committed)
.env*
!.env.example
!.env.docker.example

# embedded postgres (dev)
/.pgdata
/.docket-postgres

# local file storage driver (ticket attachments) — see lib/storage.ts.
# Docker mounts this as the docket_uploads volume instead.
/uploads

# typescript
*.tsbuildinfo
next-env.d.ts

# database dumps
*.sql.gz
*.sql.zst
*.dump
docket-db-*


# Brainer — derived index. rejected.json is your input, keep it.
.brainer/index.db
.brainer/index.db-*
.brainer/models/
.brainer/hooks/
